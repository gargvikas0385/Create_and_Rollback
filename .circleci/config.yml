version: 2.1
commands:
  destroy_environment: 
      steps:
      - run:
          name: Delete the Stack created 
          command: |
            aws cloudformation delete-stack --stack-name my-stack
jobs:
  configure_infrastructure:
    docker:
      - image: amazon/aws-cli
    steps:
      - checkout
      - run:
          name: Create infrastructure 
          command: |
             pwd && \
             ls -al && \
             aws configure --region us-west-2 --no-sign-request && \
             aws cloudformation create-stack --stack-name my-stack --template-body template.yml            
  smoke_test:
    docker:
      - image: amazon/aws-cli
    steps:
      - checkout 
      - run: return 1
      - run:
          name: Delete the Stack created 
          command: |
             aws configure --region us-west-2 --no-sign-request && \
             aws cloudformation delete-stack --stack-name my-stack
          when: on_fail

workflows:
  my_workflow:
    jobs:
      - configure_infrastructure
      - smoke_test: 
          requires: 
            - configure_infrastructure
